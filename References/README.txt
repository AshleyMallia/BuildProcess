References Experiement

1. Use <PackageReferences> so that packages directory is not even needed

** Write a script so that referencing conflicts cannot occur


Show how to efficently combine many project outputs into a single output.

- Without copying references to each project
- Without 